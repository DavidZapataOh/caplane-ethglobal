=== Vault secret created, 2026-09-10T14:58:12Z ===
  secret_id: VAULT_ROUNDTRIP_PROBE
  owner:     0xd5557A1d0806323caD77bC4049ccaca0EaDAaf06
  namespace: main

  The owner address is derived from the organization, not from a wallet key.
  It matches the value predicted before any deploy, which is what makes it
  usable as an immutable constructor argument for the registry.

--- listing (ids only, never values) ---
Loading settings...
✗ Non-interactive mode requires all inputs via flags
  → --yes
✗ missing required flags for --non-interactive mode
--- listing (ids only, never values) ---

  Waiting for authorization... (Press Ctrl+C to cancel)
  Completing vault authorization...
  Submitting request to vault gateway...
Secret identifier: secret_id=VAULT_ROUNDTRIP_PROBE, owner=0xd5557A1d0806323caD77bC4049ccaca0EaDAaf06, namespace=main

--- why the first listing attempt failed ---
  `--non-interactive` without `--yes` is rejected. Counterintuitively, BOTH flags together are
  what make browser auth work headlessly: the CLI then opens the browser instead of trying to
  draw a terminal prompt. Recorded because the obvious reading is the opposite.

=== Vault secret deleted, 2026-09-10T15:05:0xZ ===
  $ cre secrets delete <list-file>.yaml --target production-settings \
      --secrets-auth=browser --non-interactive --yes
  ✓ Secret deleted: secret_id=VAULT_ROUNDTRIP_PROBE, owner=0xd5557A1d0806323caD77bC4049ccaca0EaDAaf06, namespace=main
  $ cre secrets list ...
    No secrets found

  `delete` does not accept the same YAML as `create`. `create` takes a map id -> env var name;
  `delete` takes a list of ids. Passing secrets.yaml fails with
  `cannot unmarshal !!map into []string`.
