# ==========================================================================
# CRE PROJECT SETTINGS
# ==========================================================================
# The repository root is the CRE project root: every `cre` command resolves
# from the directory holding this file, which is what its documentation assumes.
staging-settings:
  rpcs:
    - chain-name: arc-testnet
      url: https://rpc.testnet.arc.io

production-settings:
  rpcs:
    - chain-name: arc-testnet
      url: https://rpc.testnet.arc.io
